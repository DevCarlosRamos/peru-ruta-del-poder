# Variables de entorno del proyecto.
# Copia este archivo a .env solo si necesitas sobrescribir valores por defecto.
# NO subas .env a git.

# Público: URL base de la API de guardado remoto (en Pages Functions se auto-detecta).
VITE_API_BASE=/api
